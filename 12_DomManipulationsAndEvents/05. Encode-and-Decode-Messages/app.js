function encodeAndDecodeMessages() {
    // Array.from(document.querySelectorAll('button'))
    //     .forEach(button => button.addEventListener('click', onClick));

        let encode = document.getElementsByTagName('textarea')[0];
        let decode = document.getElementsByTagName('textarea')[1];

        console.log(encode);
        console.log(decode.textContent);


//     function onClick(ev) {
//         let buttonOption = ev.target.textContent;

//         if(buttonOption === 'Encode and send it'){
//         }
//     }
}